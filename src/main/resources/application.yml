spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver

  #    url: jdbc:mysql://springboot-db.cwxzgpfpc78w.ap-northeast-2.rds.amazonaws.com:3306/myselectshop
  #    username: admin
  #    password: qhanftja1!
  #    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa: # ??? ???? ??? ?? ?? ????? ???? ??.
    hibernate:
      # ?????? ?? ??? ?? ??? ?? ???? ?? ??? ????? ??? ??? ?? ?? ????.
      ddl-auto: create-drop # ???? ???? ????? ??
    properties:
      hibernate:
        #        show_sql: true # System.out ?? SQL ??
        format_sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true # DDL ?? ? data.sql ??

logging:
  level:
    org.hibernate.SQL: debug # JPA ? Hibernate ? ???? SQL ??? ???. ( log ? ?? )
    org.hibernate.type: trace # ?? ???? ?? ( SQL ?? '?'? ???? ?? ??? ?? )